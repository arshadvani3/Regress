# Assets

Visual assets referenced by the top-level [README](../../README.md).

Currently placeholders (the README references are commented out until these
exist):

- `dashboard.png` — a screenshot of the `regress up` dashboard (Traces / Issues
  / Calibration views) with sample data. Capture by running `regress up`
  against a DB that has traces, issues, and labels, then screenshotting
  `http://localhost:8990`.
- `demo.gif` — a short screen recording of the full loop: instrument → traces
  appear → `regress cluster` → `regress evalgen` → gate catches a regression.

Once added, uncomment the corresponding `![...]` lines in the README.
